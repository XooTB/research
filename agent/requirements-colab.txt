# Extra packages for a Google Colab runtime.
#
# Colab images already ship numpy, pandas, scipy, scikit-learn, matplotlib,
# torch (CUDA build), requests and tqdm — do not pin those here, reinstalling
# them is slow and risks breaking the preinstalled CUDA wheels.
#
# Install on the runtime with:
#   colab_env.install_requirements()

lifelines>=0.28      # Cox / Kaplan-Meier for the survival endpoints in datasets/
pymupdf>=1.24        # paper_datasets_extract.py, if PDF work moves to the runtime
